#include <stdio.h>
#include <unistd.h>
#include <stdlib.h>

int main()
{
    pid_t pid = fork();

    if(pid == 0)
    {
        printf("Child Process PID: %d\n", getpid());
        printf("Child Exiting...\n");
        exit(0);
    }
    else
    {
        printf("Parent PID: %d\n", getpid());
        printf("Sleeping for 20 seconds...\n");
        sleep(20);
    }

    return 0;
}
